spring:
    profiles: test
    application:
        name: sampleapp

auth.jwt:
  appName: sampleapp
  keyStore: classpath:sample.jks
  storePassword: sample
  keyPassword: sample
  alias: sample
  algo: RSA
  trustedAppKeys:
    sampleapp: sample
  excludePaths: [ '/unsecure/**' ]    